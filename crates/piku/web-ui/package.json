{
  "name": "piku-web-ui",
  "private": true,
  "scripts": {
    "build": "esbuild app.js --bundle --format=iife --platform=browser --target=safari17 --outfile=../src/web/app.js --loader:.css=css",
    "test:e2e": "node ./node_modules/@playwright/test/cli.js test",
    "test:e2e:smoke": "node ./node_modules/@playwright/test/cli.js test e2e/operator-journey.spec.js e2e/workspace.spec.js --grep \"an operator can inspect, contextualize, rerun, and resume|a second blank-canvas click closes the creation menu|Escape dismisses the creation menu|notes drag and persist through the server|corner handles resize, reposition, and persist workspace cards|chat cards persist isolated notebook history and rerun from edited turns\"",
    "test:e2e:headed": "node ./node_modules/@playwright/test/cli.js test --headed",
    "test:harness": "node --test scripts/*.test.mjs",
    "test:agent": "node scripts/codex-playwright-test.mjs",
    "test:agent:parallel": "node scripts/parallel-agent-eval.mjs",
    "test:agent:focus-pair": "node scripts/focus-pair-eval.mjs"
  },
  "dependencies": {
    "@xterm/addon-fit": "0.11.0",
    "@xterm/xterm": "6.0.0",
    "dompurify": "^3.4.13",
    "katex": "^0.17.0",
    "marked": "^18.0.9",
    "marked-katex-extension": "^5.1.10",
    "mermaid": "^11.16.1"
  },
  "devDependencies": {
    "@playwright/mcp": "0.0.78",
    "@playwright/test": "1.58.1",
    "esbuild": "0.28.1"
  }
}
